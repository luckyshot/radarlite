<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the entire UI; this only sets the pre-draw window background/bars
         so there is no flash of a mismatched color before setContent() runs. -->
    <style name="Theme.RadarLite" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
    </style>
</resources>
